[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build -w backend"

[deploy]
startCommand = "cd backend && npm run db:migrate && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
